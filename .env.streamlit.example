# BharatVoice AI - Streamlit Web Interface Configuration
# Copy this file to .env and update the values for your environment

# Backend API Configuration
# URL of the BharatVoice backend API
# Local development: http://localhost:8000
# Production: https://api.bharatvoice.example.com
BACKEND_URL=http://localhost:8000

# Debug Mode
# Set to 'true' to enable debug information in the sidebar
# Set to 'false' in production
DEBUG=false

# Cache Configuration
# Time-to-live for cached responses in seconds (default: 3600 = 1 hour)
CACHE_TTL=3600

# Request Timeout
# Maximum time to wait for backend API responses in seconds (default: 30)
REQUEST_TIMEOUT=30

# Streamlit Server Configuration (optional, can also be set in .streamlit/config.toml)
# STREAMLIT_SERVER_PORT=8501
# STREAMLIT_SERVER_ADDRESS=localhost
# STREAMLIT_SERVER_ENABLE_CORS=false
# STREAMLIT_SERVER_ENABLE_XSRF_PROTECTION=true

# Production Environment Variables (for AWS Amplify deployment)
# Uncomment and set these when deploying to production:
# BACKEND_URL=https://api.bharatvoice.example.com
# DEBUG=false
# CACHE_TTL=7200
# REQUEST_TIMEOUT=60
